# Backend
PORT=5000

# Mongo
MONGODB_URI=mongodb://127.0.0.1:27017/team-task-manager
# Optional: explicitly set db name if your URI doesn't include it
# MONGODB_DBNAME=team-task-manager

# Auth
JWT_SECRET=replace-with-a-strong-secret

# CORS (set to your frontend dev server)
CORS_ORIGIN=http://localhost:3000

